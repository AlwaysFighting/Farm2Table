import 'package:flutter/material.dart';

class ExReviewPage extends StatelessWidget {
  const ExReviewPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("ExReviewPage"),
      ),
    );
  }
}
